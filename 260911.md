# JavaScript TIL

## 1. Date 객체

날짜와 시간을 다루는 객체

```js
const now = new Date();

now.getFullYear(); // 연도
now.getMonth() + 1; // 월 (0부터 시작)
now.getDate(); // 일

now.toLocaleString('ko-KR', {
    timeZone: 'Asia/Seoul'
});
````

* `getMonth()`는 0~11로 반환 → 실제 월은 `+1`

---

## 2. Class

객체를 생성하기 위한 설계도

```js
class Student {
    constructor(name, score) {
        this.name = name;
        this.score = score;
    }

    getInfo() {
        return `${this.name}: ${this.score}점`;
    }
}

const student = new Student('홍길동', 80);
```

* `constructor()` → 객체 생성 시 자동 실행
* `this` → 현재 객체
* `new` → 클래스의 객체 생성
* 메서드는 `prototype`을 통해 공유

---

## 3. 화살표 함수

함수를 간결하게 표현

```js
const add = (a, b) => a + b;
const square = x => x * x;
const greet = () => '안녕하세요';
```

### return

```js
const square = x => x * x; // 자동 return

const square = x => {
    return x * x;
};
```

* `{}` 사용 시 `return` 직접 작성
* `{}` 없이 작성하면 표현식의 결과가 자동 반환

### 객체 반환

```js
const createUser = () => ({
    name: '홍길동'
});
```

### this

* 화살표 함수는 **자신만의 `this`가 없음**
* 바깥 스코프의 `this`를 사용
* 콜백 함수에서 자주 사용

---

## 4. for...of

이터러블의 **값을 하나씩 순회**

```js
const fruits = ['🍎', '🍌', '🍓'];

for (const fruit of fruits) {
    console.log(fruit);
}
```

대표적인 이터러블:

* 배열
* 문자열

```js
for (const character of '안녕') {
    console.log(character);
}
```

* 일반 객체는 기본적으로 `for...of` 사용 불가

---

## 5. Spread Syntax `...`

배열/객체의 요소를 **펼침**

```js
const scores = [80, 95, 70];

Math.max(...scores);
```

### 배열 복사/결합

```js
const copy = [...scores];

const languages = ['HTML', 'CSS'];
const newLanguages = [...languages, 'JavaScript'];
```

* 새로운 배열 생성
* 객체도 사용 가능

```js
const updatedStudent = {
    ...student,
    score: 90
};
```

### 주의

Spread를 이용한 복사는 **얕은 복사**

```js
const copied = [...original];

original === copied;       // false
original[0] === copied[0]; // true (내부 객체)
```

---

## 6. Rest Parameter `...`

여러 개의 인수를 **하나의 배열로 모음**

```js
function sum(...numbers) {
    console.log(numbers);
}

sum(10, 20, 30);
// [10, 20, 30]
```

### Spread vs Rest

| Spread         | Rest        |
| -------------- | ----------- |
| 펼침             | 모음          |
| 개별 값으로 전달      | 배열로 받음      |
| 인수/배열/객체 등에 사용 | 함수 매개변수에 사용 |

```js
Math.max(...scores); // Spread

function sum(...numbers) {} // Rest
```

---

## 7. 구조 분해 할당

배열이나 객체의 값을 변수에 쉽게 할당

### 배열

```js
const colors = ['빨강', '파랑'];

const [red, blue] = colors;
```

* 배열은 **순서(인덱스)** 기준

### 기본값

```js
const [leader, assistant = '미정'] = ['홍길동'];
```

### Rest와 함께 사용

```js
const [first, ...others] = ['유관순', '장보고', '이순신'];
```

---

### 객체

```js
const student = {
    name: '유관순',
    age: 16
};

const { name, age } = student;
```

* 객체는 **프로퍼티 key 이름** 기준

### 변수명 변경

```js
const { name: learnerName } = student;
```

### 기본값

```js
const { job = '학생' } = student;
```

### 함수 매개변수에서 사용

```js
function printProduct({ name, price }) {
    console.log(name, price);
}
```

### map 콜백에서 사용

```js
products.map(({ name }) => name);
```

---

## 8. DOM 요소 선택

JavaScript로 HTML 요소를 선택하고 조작할 수 있음

### querySelector()

CSS 선택자에 맞는 **첫 번째 요소 하나**

```js
const apple = document.querySelector('#apple');
const fruit = document.querySelector('.fruit');
```

### querySelectorAll()

CSS 선택자에 맞는 **모든 요소**

```js
const fruits = document.querySelectorAll('.fruit');
```

→ `NodeList`

---

### getElementById()

id로 요소 하나 선택

```js
document.getElementById('apple');
```

### getElementsByClassName()

class로 요소들을 선택

```js
document.getElementsByClassName('fruit');
```

→ `HTMLCollection`

---

## 9. NodeList vs HTMLCollection

|           | NodeList             | HTMLCollection             |
| --------- | -------------------- | -------------------------- |
| 대표 메서드    | `querySelectorAll()` | `getElementsByClassName()` |
| 특징        | 기본적으로 고정된 목록         | Live Collection            |
| DOM 변경 반영 | X                    | O                          |

### Live Collection

DOM이 변경되면 컬렉션의 내용도 실시간으로 변경됨.

```js
const elements = document.getElementsByClassName('text');

elements[0].classList.remove('text');
```

→ `.text` 요소가 컬렉션에서 제거되므로 `elements.length`도 변경됨.

---

## 10. HTMLCollection을 배열로 변환

Spread 문법을 사용하여 배열로 변환 가능

```js
const elements = document.getElementsByClassName('text');

const array = [...elements];

array.forEach(item => {
    // ...
});
```

* `HTMLCollection` → Live Collection
* `[...elements]` → 새로운 일반 배열
* 배열로 변환하면 `map`, `filter`, `forEach` 등의 배열 메서드 활용 가능

---

# 핵심 비교

### Spread vs Rest

```text
Spread → 펼치기
Rest   → 모으기
```

### 구조 분해

```text
배열 → 순서(index) 기준
객체 → key 이름 기준
```

### DOM 선택

```text
querySelector()
→ 첫 번째 요소 하나

querySelectorAll()
→ 모든 요소
→ NodeList

getElementById()
→ id 요소 하나

getElementsByClassName()
→ class 요소들
→ HTMLCollection
→ Live Collection
```

### 화살표 함수

```text
매개변수 1개 → x => ...
매개변수 여러 개 → (a, b) => ...
매개변수 없음 → () => ...

{} 없음 → 자동 return
{} 있음 → return 필요

자신만의 this 없음
→ 바깥 스코프의 this 사용
```
