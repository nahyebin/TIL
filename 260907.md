물론! 아래 내용만 **그대로 복사해서 TIL `.md` 파일에 붙여넣으면 돼.**\
 9/7에 추가로 보내준 객체 코드까지 포함해서 정리했어.

````
# JavaScript 연산자 & 제어문 & 객체

## 오늘 배운 것
- JavaScript 연산자 종류와 사용법
- 비교 연산자 `==`, `===` 차이
- 논리 연산자와 단축 평가
- 조건문 `if`, `switch`
- 반복문 `for`, `while`, `do-while`
- `break`, `continue`를 이용한 반복문 제어
- 배열의 기본 개념과 반복문 활용
- 객체의 구조와 프로퍼티, 메서드
- 객체 프로퍼티 접근 및 추가/수정/삭제
- `for...in`을 이용한 객체 순회

## 핵심 내용

### 1. 연산자

```javascript
const a = 9;
const b = 7;

console.log(a + b);
console.log(a - b);
console.log(a * b);
console.log(a / b);
console.log(a % b);
console.log(a ** b);
````

 - 산술 연산자: `+`, `-`, `*`, `/`, `%`, `**`
- 할당 연산자: `=`, `+=`, `-=`
- 증감 연산자: `++`, `--`
- `d++` : 값을 먼저 사용한 후 증가
- `++d` : 먼저 증가한 후 값을 사용

 ### 2\. 비교 연산자

```
console.log(10 == "10");  // true
console.log(10 === "10"); // false
```

 - `==` : 값이 같은지 비교하며 필요하면 타입 변환
- `===` : 값과 타입이 모두 같은지 비교
- `!=`, `!==` : 값 또는 타입이 다른지 비교
- 일반적으로 명확한 비교를 위해 `===`, `!==` 사용

 ### 3\. 논리 연산자와 단축 평가

```
true && false; // false
true || false; // true
!true;         // false
```

 - `&&` : AND, 모두 true일 때 true
- `||` : OR, 하나라도 true면 true
- `!` : NOT, true와 false를 반대로 변경

 단축 평가에서는 결과가 이미 결정되면 나머지를 평가하지 않는다.

```
console.log('apple' || 'banana'); // apple
console.log(false || 'banana');   // banana

console.log('apple' && 'banana'); // banana
console.log(false && 'apple');    // false
```

 ### 4\. 삼항 연산자와 조건문

 삼항 연산자는 조건에 따라 두 값 중 하나를 선택한다.

```
const age = 20;
const result = age >= 20 ? "성인" : "미성년자";
```

 `if-else`는 조건에 따라 코드를 실행할 때 사용한다.

```
if (score >= 90) {
    console.log('우수');
} else if (score >= 70) {
    console.log('보통');
} else {
    console.log('부족');
}
```

 `switch`는 하나의 값을 여러 `case`와 비교할 때 사용한다.

```
switch (fruit) {
    case '사과':
        console.log('사과');
        break;
    case '바나나':
        console.log('바나나');
        break;
    default:
        console.log('알 수 없는 과일');
}
```

 ### 5\. 반복문

 - `for` : 반복 횟수가 명확할 때 사용
- `while` : 조건이 true인 동안 반복
- `do-while` : 코드를 최소 한 번 실행한 후 조건 확인

```
for (let i = 1; i <= 5; i++) {
    console.log(i);
}

let count = 3;

while (count > 0) {
    console.log(count);
    count--;
}

let number = 1;

do {
    console.log(number);
    number++;
} while (number <= 3);
```

 ### 6\. break와 continue

```
for (let i = 1; i <= 10; i++) {
    if (i % 2 === 0) {
        continue;
    }

    console.log(i);
}
```

 - `break` : 현재 반복문을 완전히 종료
- `continue` : 현재 반복만 건너뛰고 다음 반복으로 이동
- 중첩 반복문에서는 가장 가까운 반복문에 영향을 줌

 ### 7\. 배열

 배열은 여러 개의 값을 하나의 목록으로 관리한다.

```
const fruits = ['사과', '바나나', '체리'];

console.log(fruits[0]);     // 사과
console.log(fruits.length); // 3
```

 배열의 index는 `0`부터 시작하며 `length`로 요소 개수를 확인할 수 있다.

```
for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}
```

 ### 8\. 객체

 객체는 관련 있는 정보를 하나로 묶어서 관리할 수 있다.

 객체 리터럴 방식에서는 `{}`를 사용한다.

```
const student = {
    name: "판다",
    age: 5,
    getInfo: function() {
        return `${this.name}은 ${this.age}세 입니다.`;
    }
};
```

 - 프로퍼티 : 객체의 상태를 나타내는 데이터
- 프로퍼티 키 : `name`, `age`와 같은 이름
- 프로퍼티 값 : `"판다"`, `5`와 같은 실제 값
- 메서드 : 객체의 동작을 나타내는 함수
- `this` : 현재 객체 자신을 가리킴

 회원가입 정보처럼 이름, 나이, 이메일 등 서로 관련된 정보를 하나의 객체로 묶어서 관리할 수 있다.

 ### 9\. 객체 프로퍼티 접근 및 변경

 객체의 프로퍼티에 접근하는 방법은 마침표 표기법과 대괄호 표기법이 있다.

```
const dog = {
    name: '뽀삐',
    eat: function(food) {
        console.log(`${this.name}은 ${food}를 맛있게 먹어요.`);
    }
};

// 마침표 표기법
console.log(dog.name);
dog.eat('두부');

// 대괄호 표기법
console.log(dog['name']);
dog['eat']('고구마');
```

 프로퍼티 키가 변수에 저장되어 있다면 대괄호 표기법을 사용해야 한다.

```
const prop = 'name';

console.log(dog[prop]); // 뽀삐
```

 객체의 프로퍼티는 추가, 수정, 삭제할 수 있다.

```
dog.name = '감자'; // 수정
dog.age = 5;      // 추가
delete dog.age;   // 삭제
```

 `const`로 선언한 객체도 내부 프로퍼티를 변경하는 것은 가능하지만 객체 자체를 재할당할 수는 없다.

 ### 10\. 객체 프로퍼티 확인 및 순회

 `in` 연산자를 사용하면 객체에 특정 프로퍼티가 존재하는지 확인할 수 있다.

```
console.log('name' in dog);   // true
console.log('weight' in dog); // false
```

 `for...in`은 객체의 프로퍼티 키를 하나씩 꺼내서 순회할 때 사용한다.

```
for (const key in dog) {
    console.log(`키: ${key}`);
    console.log(`값: ${dog[key]}`);
}
```

 객체의 프로퍼티 키가 변수에 들어있기 때문에 `dog[key]`처럼 대괄호 표기법을 사용한다.

