 ## JavaScript 연산자 & 제어문

 ## 오늘 배운 것

 - 산술, 할당, 증감 연산자
- 비교 연산자와 `==`, `===` 차이
- 논리 연산자와 단축 평가
- 삼항 연산자
- `if-else` 조건문
- `switch` 조건문
- `for` 반복문
- `while`, `do-while` 반복문
- `break`, `continue`
- 배열과 객체의 기본 개념

 ## 핵심 내용

 ### 1\. 산술 / 할당 / 증감 연산자

```
const a = 9;
const b = 7;

console.log(a + b); // 덧셈
console.log(a - b); // 뺄셈
console.log(a * b); // 곱셈
console.log(a / b); // 나눗셈
console.log(a % b); // 나머지
console.log(a ** b); // 거듭제곱
```

 - 산술 연산자: `+`, `-`, `*`, `/`, `%`, `**`
- 할당 연산자: `=`, `+=`, `-=` 등
- 증감 연산자: `++` 1 증가, `--` 1 감소
- 변수 선언 후 재할당이 필요한 경우 `let` 사용

 ### 2\. 전위 / 후위 증감 연산자

```
let d = 5;

console.log(d++); // 5 → 사용 후 증가
console.log(++d); // 7 → 증가 후 사용
```

 - `d++` : 현재 값을 먼저 사용하고 1 증가
- `++d` : 먼저 1 증가시키고 변경된 값을 사용
- `d--`, `--d`도 같은 방식으로 동작

 ### 3\. 비교 연산자

```
x > y
x < y
x >= y
x <= y
x == y
x != y
x === y
x !== y
```

 - 비교 연산자의 결과는 `true` 또는 `false`
- `==` : 값만 비교하며 필요하면 타입 변환
- `===` : 값과 타입을 모두 비교
- 일반적으로 **`===`, `!==` 사용을 권장**

```
10 == "10";  // true
10 === "10"; // false
```

 ### 4\. 논리 연산자와 단축 평가

```
true && false; // false
true || false; // true
!true;         // false
```

 - `&&` : AND, 모두 `true`여야 `true`
- `||` : OR, 하나라도 `true`면 `true`
- `!` : NOT, 논리값을 반대로 변경

 단축 평가에서는 조건의 결과가 확정되면 나머지를 평가하지 않는다.

```
console.log('apple' || 'banana'); // apple
console.log(false || 'banana');   // banana

console.log('apple' && 'banana'); // banana
console.log(false && 'apple');     // false
```

 ### 5\. 삼항 연산자

 조건에 따라 두 값 중 하나를 선택할 때 사용한다.

```
const age = 20;

const result = age >= 20 ? "성인" : "미성년자";
console.log(result);
```

 기본 형태:

```
조건 ? 참일 때 값 : 거짓일 때 값
```

 간단한 `if-else`를 짧게 표현할 때 사용할 수 있다.

 ### 6\. if 조건문

```
if (조건식) {
    // 조건이 true일 때
} else if (조건식) {
    // 위 조건이 false이고 현재 조건이 true일 때
} else {
    // 모든 조건이 false일 때
}
```

 여러 조건을 순서대로 확인할 때 `if → else if → else`를 사용한다.

```
let score = 75;

if (score >= 90) {
    console.log('우수');
} else if (score >= 70) {
    console.log('보통');
} else {
    console.log('부족');
}
```

 ### 7\. switch문

 하나의 값을 여러 경우와 비교할 때 사용한다.

```
const fruit = '바나나';

switch (fruit) {
    case '사과':
        console.log('사과');
        break;
    case '바나나':
        console.log('바나나');
        break;
    default:
        console.log('알 수 없는 과일');
}
```

 - `case` : 특정 값과 일치하는 경우
- `break` : 해당 `switch`문을 종료
- `default` : 어떤 `case`에도 해당하지 않을 때 실행

 ### 8\. for 반복문

 반복 횟수가 명확할 때 사용한다.

```
for (초기화; 조건식; 증감식) {
    // 반복할 코드
}
```

```
for (let i = 1; i <= 5; i++) {
    console.log(i);
}
```

 실행 순서:

```
초기화 → 조건식 확인 → 실행 → 증감식 → 조건식 확인 → ...
```

 배열의 요소를 반복해서 확인할 때도 사용할 수 있다.

```
const fruits = ['사과', '바나나', '체리'];

for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}
```

 ### 9\. while / do-while

 `while`은 **조건이 참인 동안 반복**한다.

```
let count = 3;

while (count > 0) {
    console.log(count);
    count--;
}
```

 `do-while`은 **코드를 최소 한 번 실행한 후 조건을 확인**한다.

```
let number = 1;

do {
    console.log(number);
    number++;
} while (number <= 3);
```

 - `for` : 반복 횟수가 명확할 때
- `while` : 반복을 멈출 조건이 중요할 때
- `do-while` : 조건과 관계없이 최소 한 번 실행해야 할 때

 ### 10\. 배열 / 객체 / break / continue

 **배열**은 여러 개의 값을 하나의 목록으로 관리한다.

```
const fruits = ['사과', '바나나', '체리'];

console.log(fruits[0]); // 사과
console.log(fruits.length); // 3
```

 **객체**는 관련 있는 정보를 하나로 묶어서 관리한다.

```
const user = {
    name: 'panda',
    age: 20,
    email: 'example@email.com'
};
```

 회원가입처럼 한 사람의 이름, 나이, 이메일 등의 정보를 각각의 변수로 관리하기보다 **회원 한 명에 대한 정보를 객체로 묶어서 관리**할 수 있다.

 `continue`와 `break`는 반복문의 흐름을 제어한다.

```
for (let i = 1; i <= 10; i++) {
    if (i % 2 === 0) {
        continue;
    }

    console.log(i);
}
```

 - `continue` : 현재 반복의 나머지 코드를 건너뛰고 다음 반복으로 이동
- `break` : 현재 속한 반복문을 완전히 종료
- 중첩 반복문에서 `continue`, `break`는 **가장 가까운 반복문에 적용**된다.