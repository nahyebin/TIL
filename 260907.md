## 연산자
### 자바스크립트에서 자주 사용되는 연산자는 산술, 할당, 증감 연산자 등이 있다.

### 1. 산술연산자
```
console.log(a + b);
console.log(a - b);
console.log(a * b);
console.log(a / b); // 몫
console.log(a % b); // 나머지
console.log(a ** b); // 제곱
```

### 2. 할당연산자
```
let c = 5;
c += 3; // c = c + 3
c -= 2; // c = c - 2
```

### 3. 증감연산자
```
++는 1 증가
--는 1 감소

후위 증감 : d++ : 현재 값을 먼저 사용, 그 다음 값을 변경
전위 증감 : ++d : 값을 먼저 변경, 바뀐 값을 사용
```

## 비교 연산자 (>, <, >=, <=)
### 좌항과 우항의 비교 연산자를 비교하고 그 결과를 boolean값으로 반환
### if나 for문과 같은 제어문의 조건식에서 주로 사용

## 동등/일치 비교 연산자
### 동등 비교(==, !=) : 먼저 암묵적 타입 변환을 통해 타입을 일치시킨 후 비교
### 일치 비교(===, !==) : 타입과 값이 모두 일치하는지 비교
```
const x = 10;
const y = 20;

동등연산자
console.log(x == y);  // == : 값이 같으면 true (출력: false)
console.log(x != y)  // != : 같이 다르면 true (출력: true)

일치연산자 : 타입까지 비교
console.log(x === 10); // 값과 타입이 같으면 true
console.log(x === "10"); // false
console.log(x == "10"); // true

비교 연산자를 통해 문자열끼리 비교도 가능하며 유니코드 순으로 비교한다.
console.log(`'hello' === 'hello' : ${'hello' === 'hello'}`); // true
```
## 논리연산자 (AND, OR, NOT)
### 여러 조건을 결합하여 하나의 논리적 결과를 도출한