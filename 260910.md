# JavaScript 배열, 문자열, Math, 화살표 함수

## 1. 배열 (Array)

배열은 여러 개의 값을 하나의 목록으로 묶어서 관리하는 자료구조이다.

```js
const fruits = ['바나나', '복숭아', '키위'];

console.log(fruits[0]);      // 바나나
console.log(fruits.length);  // 3
````

* 인덱스는 **0부터 시작**
* 존재하지 않는 인덱스에 접근하면 `undefined`
* `length`는 배열의 요소 개수
* 배열인지 확인: `Array.isArray(array)`

### 배열 기본 메서드

| 메서드          | 설명          | 반환값              |
| ------------ | ----------- | ---------------- |
| `push()`     | 배열 끝에 요소 추가 | 변경된 length       |
| `pop()`      | 배열 끝 요소 제거  | 제거한 요소           |
| `unshift()`  | 배열 앞에 요소 추가 | 변경된 length       |
| `shift()`    | 배열 앞 요소 제거  | 제거한 요소           |
| `indexOf()`  | 값의 인덱스 검색   | 인덱스 / 없으면 `-1`   |
| `includes()` | 값이 존재하는지 확인 | `true` / `false` |

> `push`, `pop`, `unshift`, `shift`는 원본 배열을 변경한다.

---

## 2. 배열 순회 및 변환

### forEach()

배열의 각 요소를 하나씩 처리할 때 사용한다.

```js
students.forEach(student => {
    console.log(student.name);
});
```

* 각 요소를 순회
* 반환값을 모아 새로운 배열을 만들지 않음
* `forEach()` 자체의 반환값은 `undefined`
* 콜백에는 `요소`, `인덱스` 등을 전달할 수 있음

### map()

각 요소를 다른 값으로 변환하여 **새로운 배열**을 만들 때 사용한다.

```js
const names = students.map(student => student.name);
```

### forEach vs map

* `forEach` → 배열의 요소를 가지고 **작업**
* `map` → 배열의 요소를 **변환해서 새로운 배열 생성**

---

### filter()

조건을 만족하는 **모든 요소**를 새로운 배열로 반환한다.

```js
const highScorers = students.filter(
    student => student.score >= 85
);
```

조건에 맞는 요소가 없으면 `[]` 반환.

### find()

조건을 만족하는 **첫 번째 요소 하나**를 반환한다.

```js
const student = students.find(
    student => student.score >= 85
);
```

조건에 맞는 요소가 없으면 `undefined` 반환.

### filter vs find

* `filter()` → 조건에 맞는 **전체** → 배열
* `find()` → 조건에 맞는 **첫 번째 하나** → 요소

---

### some()

조건을 만족하는 요소가 **하나라도 있는지** 확인한다.

```js
students.some(student => student.score >= 90);
```

→ `true` / `false`

### every()

**모든 요소**가 조건을 만족하는지 확인한다.

```js
students.every(student => student.score >= 80);
```

→ `true` / `false`

### some vs every

* `some()` → 하나라도 만족하면 `true`
* `every()` → 전부 만족해야 `true`

---

### sort()

배열을 정렬하며 **원본 배열을 변경한다.**

숫자 오름차순:

```js
numbers.sort((a, b) => a - b);
```

숫자 내림차순:

```js
numbers.sort((a, b) => b - a);
```

* `a - b` → 오름차순
* `b - a` → 내림차순

---

### reduce()

배열의 값을 하나의 결과로 누적할 때 사용한다.

```js
const total = amounts.reduce((sum, current) => {
    return sum + current;
}, 0);
```

```text
sum     → 누적값
current → 현재 요소
0       → 초기값
```

---

## 3. 문자열 (String)

문자열은 **변경할 수 없는 값(immutable)**이다.

문자열 메서드를 사용해도 원본이 변경되지 않고 새로운 값을 반환한다.

### 주요 메서드

| 메서드             | 설명                    |
| --------------- | --------------------- |
| `trim()`        | 앞뒤 공백 제거              |
| `toLowerCase()` | 소문자로 변환               |
| `toUpperCase()` | 대문자로 변환               |
| `indexOf()`     | 문자열의 위치 검색 / 없으면 `-1` |
| `includes()`    | 문자열 포함 여부 확인          |
| `slice()`       | 문자열 일부 추출             |
| `lastIndexOf()` | 마지막으로 등장한 위치 검색       |
| `split()`       | 문자열을 기준에 따라 배열로 분리    |

### slice()

```js
const fileName = 'my-profile.jpg';

fileName.slice(0, 10);
```

* 시작 인덱스부터
* **끝 인덱스 바로 앞까지** 추출

### split()

문자열을 배열로 변환할 때 사용한다.

```js
const tags = '#자바스크립트#개발자#프론트엔드';

const tagList = tags
    .split('#')
    .filter(tag => tag !== '');
```

`split()`과 `filter()`처럼 메서드를 연속해서 사용하는 것을 **메서드 체이닝**이라고 한다.

---

## 4. Math

수학 관련 기능을 제공하는 표준 빌트인 객체이다.

### 반올림 / 내림 / 올림

```js
Math.round(3.6); // 4
Math.floor(3.6); // 3
Math.ceil(3.1);  // 4
```

* `round()` → 반올림
* `floor()` → 내림
* `ceil()` → 올림

### Math.random()

`0 이상 1 미만`의 랜덤한 실수를 반환한다.

```js
Math.random();
```

### 랜덤 정수

```js
Math.floor(Math.random() * 10);
```

→ `0 ~ 9`

```js
Math.floor(Math.random() * 10) + 1;
```

→ `1 ~ 10`

---

## 5. 화살표 함수 (Arrow Function)

기존 함수 표현식을 간단하게 작성할 수 있다.

```js
const power = function(x) {
    return x * x;
};

const arrowPower = x => x * x;
```

### 기본 문법

```js
// 매개변수 1개
const square = x => x * x;

// 매개변수 여러 개
const add = (a, b) => a + b;

// 매개변수 없음
const greet = () => '안녕하세요';
```

### return 주의

중괄호 `{}` 없이 작성하면 반환값이 자동으로 반환된다.

```js
const square = x => x * x;
```

중괄호를 사용하면 `return`을 직접 작성해야 한다.

```js
const square = x => {
    return x * x;
};
```

```js
const wrongSquare = x => {
    x * x;
};

console.log(wrongSquare(3)); // undefined
```

### 객체 반환

화살표 함수에서 객체를 바로 반환하려면 `()`로 감싼다.

```js
const createUser = (id, name) => ({
    id: id,
    name: name
});
```

---

## 6. 콜백 함수

**다른 함수에 인자로 전달되어 실행되는 함수**를 콜백 함수라고 한다.

```js
students.forEach(student => {
    console.log(student.name);
});
```

여기서

```js
student => {
    console.log(student.name);
}
```

가 콜백 함수이다.

`forEach()`, `map()`, `filter()`, `find()` 등에서 자주 사용한다.

---

## ⭐ 오늘의 핵심 암기

```text
push    → 뒤에 추가
pop     → 뒤에서 제거
unshift → 앞에 추가
shift   → 앞에서 제거

indexOf  → 위치 찾기
includes → 존재 여부

forEach → 각각 작업
map     → 변환해서 새 배열
filter  → 조건에 맞는 전체
find    → 조건에 맞는 첫 번째
some    → 하나라도 만족?
every   → 모두 만족?
reduce  → 누적해서 하나의 값
sort    → 정렬

trim        → 앞뒤 공백 제거
toLowerCase → 소문자
toUpperCase → 대문자
slice       → 문자열 일부 추출
split       → 문자열 → 배열

round → 반올림
floor → 내림
ceil  → 올림
random → 0 이상 1 미만 랜덤

화살표 함수
x => x * x
→ 중괄호가 없으면 자동 return
→ 중괄호가 있으면 return 필요
```
