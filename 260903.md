## HTML & CSS

## 오늘 배운 것
- HTML, CSS 기본 개념
- HTML 기본 문법과 주요 태그
- CSS 기본 문법과 선택자

## 핵심 내용 (코드)
### 1. HTML, CSS, JavaScript 개념
HTML은 웹 페이지를 만들기 위한 가장 기본적인 구조와 뼈대를 담당한다.

CSS는 HTML로 만든 구조를 꾸미고 디자인하는 역할을 한다.

JavaScript는 버튼 클릭, 자동문처럼 웹 페이지에서 발생하는 동작과 기능을 담당한다.

쉽게 생각하면:
```
HTML = 뼈대
CSS = 꾸미기
JavaScript = 동작
```
### 2. HTML 기본 구조
VS Code에서 !을 입력하고 Enter를 누르면 HTML의 기본 구조가 자동으로 생성된다.
```
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>페이지 제목</title>
</head>
<body>

</body>
</html>
```
head는 페이지의 설정이나 정보를 작성하는 부분이고, body는 실제 웹 페이지에서 사용자에게 보이는 내용을 작성하는 부분이다.

HTML 주석은 다음과 같이 작성한다.
```
<!-- 주석 내용 -->
```
VS Code에서는 Ctrl + /를 사용하면 주석을 쉽게 작성할 수 있다.

### 3. 기본 HTML 태그
제목은 h1부터 h6까지 사용할 수 있으며 숫자가 커질수록 기본 글자 크기가 작아진다.
```
<h1>가장 큰 제목</h1>
<h2>두 번째 제목</h2>
<h3>세 번째 제목</h3>
```
문단은 p 태그를 사용하고, 줄바꿈은 br, 구분선은 hr을 사용한다.
```
<p>문단입니다.</p>
```
```
<br>
<hr>
```
br과 hr은 별도의 닫는 태그가 없다.

pre 태그는 p 태그와 달리 작성한 공백과 줄바꿈을 그대로 보여준다. 주로 코드를 표현할 때 사용할 수 있다.
```
<pre>
    작성한 모양이
        그대로 보여집니다.
</pre>
```
### 4. 글자 강조 태그
```
<b>굵은 글씨</b>
<strong>중요한 내용</strong>

<i>기울인 글씨</i>
<em>강조하는 내용</em>

<mark>형광펜 효과</mark>
<del>삭제된 내용</del>
```
b와 i는 주로 단순한 시각적인 표현이고, strong과 em은 글자에 의미를 부여한다는 차이가 있다.

### 5. 목록
순서가 없는 목록은 ul, 순서가 있는 목록은 ol을 사용한다.

각각의 항목은 li 태그로 작성한다.
```
<ul>
    <li>우유</li>
    <li>계란</li>
    <li>사과</li>
</ul>

<ol>
    <li>물을 끓인다</li>
    <li>면과 스프를 넣는다</li>
    <li>맛있게 먹는다</li>
</ol>

ul = 순서 X
ol = 순서 O
li = 목록 항목
```
### 6. 표 만들기
표는 table 태그를 사용한다.
```
<table>
    <caption>웹 개발 스터디 시간표</caption>

    <thead>
        <tr>
            <th>시간</th>
            <th>월요일</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>1교시</td>
            <td>HTML 기초</td>
        </tr>
    </tbody>
</table>
```
각 태그의 역할은 다음과 같다.
```
table = 표 전체
caption = 표 제목
thead = 표의 머리 부분
tbody = 표의 본문
tr = 행
th = 제목 셀
td = 일반 셀
```
rowspan은 세로 방향으로 셀을 합칠 때 사용하고, colspan은 가로 방향으로 셀을 합칠 때 사용한다.
```
<td rowspan="2">HTML 기초</td>
<td colspan="2">미니 프로젝트</td>
```
### 7. 이미지와 미디어
이미지는 img 태그를 사용한다.
```
<img
    src="sample/image/flower.PNG"
    alt="꽃 사진"
    width="300"
    height="200"
>
```
src는 이미지 파일의 위치, alt는 이미지 설명, width와 height는 이미지 크기를 지정한다.

이미지와 설명을 함께 표현할 때는 figure와 figcaption을 사용할 수 있다.
```
<figure>
    <img src="city.jpg" alt="도시 풍경">
    <figcaption>도시 풍경 사진입니다.</figcaption>
</figure>
```
오디오는 audio, 비디오는 video 태그를 사용한다.
```
<audio src="major.mp3" controls></audio>

<video src="video1.mp4" controls></video>
```
controls 속성을 사용하면 재생, 정지, 볼륨 조절 등의 미디어 조작 버튼이 화면에 표시된다.

### 8. 하이퍼링크
다른 웹사이트로 이동할 때는 a 태그를 사용한다.
```
<a href="https://www.google.com">구글로 이동</a>
```
새로운 탭에서 열고 싶다면 target="_blank"를 사용할 수 있다.
```
<a href="https://www.naver.com" target="_blank">
    네이버로 이동
</a>
```
같은 폴더의 다른 HTML 파일로 이동할 수도 있다.
```
<a href="01_text-and-list.html">1번 파일</a>
```
페이지 내부의 특정 위치로 이동할 때는 id와 #을 사용한다.
```
<a href="#guestbook">방명록으로 이동</a>

<legend id="guestbook">방명록 작성</legend>
```
즉,
```
href="#guestbook"
        ↓
id="guestbook"
```
처럼 연결된다.

### 9. Form
form은 사용자가 입력한 데이터를 서버로 보내기 위한 창구 역할을 한다.
```
<form action="#" method="post">
    ...
</form>
```
action은 데이터를 보낼 서버 주소를 지정하고, method는 데이터를 어떤 방식으로 전달할지 지정한다.

입력 요소에는 input, select, textarea 등을 사용할 수 있다.
```
<input
    type="text"
    id="user-id"
    name="userId"
    placeholder="아이디를 입력하세요"
    required
>
```
주요 속성은 다음과 같다.
```
id = HTML에서 특정 요소 식별
name = 서버로 데이터를 보낼 때 사용하는 이름
value = 실제 전달되는 값
placeholder = 입력하기 전 안내 문구
required = 필수 입력
checked = 기본 선택
```
특히 id와 name의 차이를 구분하는 것이 중요했다.

id는 HTML 안에서 특정 요소를 식별하기 위한 것이고, name은 Form 데이터를 서버로 전송할 때 사용되는 이름이다.

### 10. Label과 Radio / Checkbox
label의 for와 input의 id를 연결할 수 있다.
```
<label for="user-id">아이디:</label>
<input id="user-id" type="text">
```
이렇게 연결하면 사용성과 접근성이 좋아진다. (버튼 클릭 X 글씨 클릭해도 선택 O)

Radio 버튼은 여러 항목 중 하나만 선택할 때 사용한다.
```
<input type="radio" id="route-internet" name="route" value="internet">
<label for="route-internet">인터넷 검색</label>

<input type="radio" id="route-friend" name="route" value="friend">
<label for="route-friend">친구 추천</label>
```
Radio 버튼에서 name이 같으면 같은 그룹으로 묶이기 때문에 하나만 선택할 수 있다.

예를 들어 인터넷 검색을 선택하면:
```
route=internet
```
형태로 서버에 데이터가 전달된다.

반대로 Checkbox는 여러 항목을 동시에 선택할 수 있다.
```
<input type="checkbox" name="hobby" value="html">
<input type="checkbox" name="hobby" value="css">
<input type="checkbox" name="hobby" value="js">
```
### 11. textarea와 버튼
여러 줄의 내용을 입력할 때는 textarea를 사용한다.
```
<textarea name="memo" rows="5"></textarea>
```
rows는 처음 화면에 보여지는 입력 영역의 높이를 설정한다.

Form을 제출할 때는 submit, 입력 내용을 초기화할 때는 reset을 사용한다.
```
<button type="submit">작성 완료</button>
<button type="reset">다시 작성</button>
```
Form을 제출한 후 F12 개발자 도구에서 Network → Payload를 확인하면 실제로 서버에 전달되는 데이터를 확인할 수 있다.

### 12. div와 span
div와 span은 HTML에서 영역을 나누거나 특정 부분을 묶을 때 사용하는 태그이다.

div는 기본적으로 블록 레벨 요소라 한 줄을 차지하고, span은 인라인 요소라 콘텐츠 크기만큼 공간을 차지한다.
```
<div>첫 번째 영역</div>
<div>두 번째 영역</div>

<span>첫 번째</span>
<span>두 번째</span>
```
### 13. 시맨틱 태그
예전에는 웹 페이지의 대부분을 div로 구성하기도 했지만, 모든 영역을 div로 만들면 코드의 의미를 파악하기 어렵고 유지보수가 복잡해질 수 있다.

그래서 각 영역의 의미를 나타내는 시맨틱 태그를 사용한다.
```
<header>
    <nav>메뉴</nav>
</header>

<main>
    <section>
        본문 내용
    </section>
</main>

<footer>
    하단 내용
</footer>
```
대표적인 시맨틱 태그는 다음과 같다.
```
header = 머리말
nav = 메뉴/네비게이션
main = 핵심 내용
section = 콘텐츠 구역
footer = 하단 영역
```
시맨틱 태그를 사용하면 웹 페이지의 구조와 의미를 더 명확하게 표현할 수 있고, 유지보수와 접근성 및 SEO에도 도움이 된다.

### 14. CSS 기본 개념
CSS는 HTML로 만들어진 웹 페이지에 디자인을 적용하는 언어이다.

기본 문법은 다음과 같다.
```
선택자 {
    속성: 값;
}

예를 들어:

h2 {
    color: red;
    font-size: 30px;
}
```
여기서 h2는 선택자, color와 font-size는 속성, red와 30px은 값이다.

CSS를 적용하는 방법은 크게 세 가지가 있다.
```
1. 인라인 스타일
2. 내부 스타일
3. 외부 스타일
```
외부 CSS 파일을 연결할 때는 다음과 같이 사용한다.
```
<link rel="stylesheet" href="style.css">
```
실무에서는 HTML과 CSS를 분리하여 관리하는 외부 스타일 시트를 많이 사용한다.

### 15. CSS 선택자
CSS에서는 내가 꾸미고 싶은 요소를 정확하게 선택하는 것이 중요하다.

태그 선택자는 태그 이름을 그대로 사용한다.
```
h2 {
    color: red;
}
```
클래스 선택자는 .으로 시작한다.
```
.highlight-text {
    background-color: aqua;
}
```
클래스는 여러 요소에 반복해서 사용할 수 있다.

아이디 선택자는 #으로 시작한다.
```
#blue-text {
    color: blue;
}
```
아이디는 특정 요소를 식별하기 위한 것이므로 반복적으로 같은 스타일을 적용할 때는 클래스를 사용하는 것이 적절하다.
```
클래스 = 여러 요소에 반복 사용
아이디 = 특정 요소 식별
```
### 16. 다양한 CSS 선택자
후손 선택자는 띄어쓰기를 사용한다.
```
#lecture-list li {
    font-weight: bold;
}
```
#lecture-list 안에 있는 모든 li를 선택한다.

자손 선택자는 >를 사용한다.
```
body > p {
    font-size: 30px;
}
```
body의 바로 아래에 있는 p만 선택한다.

속성 선택자는 특정 속성을 가진 요소를 선택한다.
```
input[type="text"] {
    border: 2px solid orange;
}
```
type="text"인 input만 선택한다.

### 17. :checked와 인접 형제 선택자
Radio 버튼이 선택되었을 때 바로 뒤에 있는 label을 꾸미는 코드도 실습했다.
```
input[type="radio"]:checked + label {
    font-weight: bold;
    color: blueviolet;
}
```
:checked는 현재 선택된 radio나 checkbox를 의미한다.

+는 바로 뒤에 이어지는 형제 요소를 선택한다.

따라서 위 코드는:
```
선택된 radio
      ↓
바로 뒤의 label
      ↓
굵게 + 보라색
```
으로 이해할 수 있다.

후손 선택자와 자손 선택자의 차이도 중요했다.
```
A B     = A 안의 모든 B
A > B   = A 바로 아래의 B
```
### 18. CSS 선택자 우선순위
기본적으로 배운 선택자의 우선순위는 다음과 같다.

태그 < 클래스 < 아이디

즉, 일반적으로 ID 선택자가 클래스 선택자보다 우선순위가 높고, 클래스 선택자가 태그 선택자보다 우선순위가 높다.

### 19. 글자와 상자(Box Model)
CSS에서는 글자의 모양뿐만 아니라 HTML 요소가 차지하는 상자의 크기와 간격도 조절할 수 있다.

대표적인 글자 관련 속성은:
```
color: red;
font-size: 30px;
font-weight: bold;
font-style: italic;
font-family: sans-serif;
```
그리고 CSS에서 중요한 개념 중 하나가 Box Model이다.
```
Content
   ↓
Padding
   ↓
Border
   ↓
Margin
```
Content → 실제 내용
Padding → 내용과 테두리 사이의 안쪽 여백
Border → 테두리
Margin → 다른 요소와의 바깥 여백



## 느낀점
오늘 HTML과 CSS를 처음부터 직접 작성하면서 웹 페이지가 어떤 구조로 만들어지는지 조금 더 구체적으로 이해할 수 있었다. HTML 태그가 header, main, section, footer 같은 시맨틱 태그를 사용하면서 각각의 영역에 의미를 부여할 수 있다는 것을 알게 되었다.

Radio 버튼에서 name이 같아야 하나만 선택된다는 것을 알게 되었다. 또한 개발자 도구의 Network → Payload를 통해 실제로 입력한 데이터가 어떤 형태로 서버에 전달되는지도 확인해볼 수 있었다.

CSS에서는 단순히 색깔이나 글자 크기를 변경하는 것뿐만 아니라 선택자를 이용해 원하는 요소를 정확하게 선택하는 것이 중요하다는 것을 배웠다. 특히 태그, 클래스, ID 선택자의 차이와 후손/자손 선택자의 차이를 직접 코드로 확인하면서 조금 더 이해할 수 있었다.
