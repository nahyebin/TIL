# CSS & JavaScript 기초

## 오늘 배운 것

 - CSS 박스모델
- `box-sizing`, `border`, `box-shadow`
- Flexbox와 Grid를 이용한 레이아웃
- Position과 가상 클래스
- JavaScript와 ECMAScript
- 변수와 `let`, `const`
- JavaScript 자료형
- 템플릿 리터럴
- 암묵적/명시적 타입 변환
- Truthy, Falsy와 `typeof`

 ## 핵심 내용

 ### 1\. CSS 박스모델

 - `padding` : 테두리 안쪽 여백
- `margin` : 테두리 바깥쪽 여백
- `border` : 테두리
- `box-sizing: border-box` : `width`, `height`에 padding과 border를 포함하여 계산

 ### 2\. CSS 기타 속성

```
border: 2px solid black;
border-radius: 15px;
box-shadow: 10px 10px 15px gray;
```

 - `border-radius` : 모서리를 둥글게
- `box-shadow` : 요소에 그림자 적용

 ### 3\. Flexbox

```
display: flex;
justify-content: center;
align-items: center;
flex-wrap: wrap;
```

 - Flexbox는 한 방향으로 요소를 배치할 때 사용
- `justify-content` : 주축 방향 정렬
- `align-items` : 교차축 방향 정렬
- `flex-wrap` : 공간이 부족할 때 줄바꿈

 ### 4\. Grid

```
display: grid;
grid-template-columns: 1fr 1fr 1fr;
gap: 10px;
```

 - Grid는 행과 열을 함께 사용하는 2차원 레이아웃
- `1fr 1fr 1fr` : 공간을 1:1:1 비율로 나눔
- `gap` : 요소 사이의 간격

 ### 5\. Position & 가상 클래스

```
.parent {
    position: relative;
}

.child {
    position: absolute;
    top: 50px;
    left: 50px;
}

.button:hover {
    background-color: blue;
}

.button:active {
    transform: translateY(-2px);
}
```

 - `relative` : 자식 요소의 위치 기준점으로 사용
- `absolute` : 기준점을 기준으로 원하는 위치에 배치
- `:hover` : 마우스를 올렸을 때
- `:active` : 클릭하고 있는 동안

 ### 6\. JavaScript와 ECMAScript

 - JavaScript는 사용자의 행동에 반응하는 웹 페이지를 만드는 데 사용
- ES5, ES6는 ECMAScript의 버전을 의미
- Node.js를 이용하면 브라우저 밖에서도 JavaScript 실행 가능

 ### 7\. 변수와 `let`, `const`

```
let greeting = "Hello";
greeting = "Welcome";

const num = 1;
// num = 2; // 재할당 불가
```

 - 변수 : 데이터를 저장하는 이름이 붙은 저장소
- `let` : 재할당 가능
- `const` : 재할당 불가
- `var` : 과거에 주로 사용하던 변수 선언 방식
- 재할당하지 않는 변수는 `const`를 우선적으로 사용

 ### 8\. JavaScript 자료형

```
const age = 30;          // Number
const name = "panda";    // String
const isStudent = true;  // Boolean
const address = null;    // null
let salary;              // undefined
```

 - `Number` : 숫자
- `String` : 문자열
- `Boolean` : `true`, `false`
- `null` : 의도적으로 값이 없음을 나타냄
- `undefined` : 값이 할당되지 않은 상태
- `typeof` : 자료형을 확인할 때 사용

 ### 9\. 템플릿 리터럴

```
const lastName = "권";
const firstName = "은지";

console.log(`제 이름은 ${lastName}${firstName}입니다.`);
```

 - 백틱(\`)을 사용
- `${}`를 이용해 문자열 안에 변수나 표현식을 삽입
- 여러 줄의 문자열 작성 가능

 ### 10\. 타입 변환과 Truthy/Falsy

```
10 + '20';           // '1020'
10 - '5';            // 5

Number('10.01');     // 10.01
parseInt('10.01');   // 10
parseFloat('10.01'); // 10.01
```

 - **암묵적 타입 변환** : JavaScript가 자동으로 타입을 변환
- **명시적 타입 변환** : 개발자가 직접 타입을 변환
- 문자열이 포함된 `+` 연산은 문자열 연결로 동작할 수 있음
- Falsy 값은 다음 6가지

```
false
0
''
null
undefined
NaN
```

 - Falsy가 아닌 대부분의 값은 Truthy로 평가됨
- `!!값`을 사용하면 해당 값의 Truthy/Falsy 여부를 확인할 수 있음