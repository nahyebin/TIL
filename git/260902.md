## Git & Github

## 오늘 배운 것
- Git, Github 개념
- GitHub 원격 저장소 연결 및 push, pull
- Branch 생성 및 이동
- Branch merge와 Merge Conflict 해결
- Fork, Clone, Pull Request 이용

## 핵심 내용 (코드)

### 1. Git 기본 설정
```
git --version

git config --global user.name "깃허브사용이름"
git config --global user.email "깃허브사용이메일"

git config --list
```
Git을 사용하기 전에 커밋을 작성할 사용자의 이름과 이메일을 설정한다.

### 2. Git 저장소 생성
```
git init
```

현재 폴더를 Git으로 버전 관리할 수 있는 저장소로 만든다.

git init을 실행하면 .git 폴더가 생성된다.

### 3. 파일 상태 확인 및 Commit
```
git status

git add hello.txt
git add .

git commit -m "첫번째 텍스트 파일 생성"

git log
git log --oneline
```
Git의 기본적인 변경 이력 저장 과정은 다음과 같다.
```
Working Directory
      ↓ git add
Staging Area
      ↓ git commit
Local Repository
```
```
git status : 현재 파일의 상태 확인
git add : 변경사항을 Staging Area에 추가
git commit : 변경사항을 하나의 이력으로 저장
git log : 커밋 이력 확인
```
### 4. Commit 되돌리기
```
git revert 커밋ID
```
기존 커밋을 삭제하는 것이 아니라, 해당 커밋의 변경사항을 되돌리는 새로운 커밋을 생성한다.

### 5. GitHub 원격 저장소 연결
```
git remote add origin https://github.com/nahyebin/git-practice-nhb.git

git remote -v
```
```
origin : 원격 저장소의 이름
git remote -v : 원격 저장소 연결 상태 확인
```
### 6. Push와 Pull
```
git push -u origin main

git push

git pull origin main
```
```
Local Repository
      ↓ git push
GitHub Repository

GitHub Repository
      ↓ git pull
Local Repository
```
```
push : 로컬의 변경사항을 GitHub에 업로드
pull : GitHub의 변경사항을 로컬로 가져오기
-u : 처음 push할 때 로컬 브랜치와 원격 브랜치를 연결
```
### 7. Branch
```
git branch feature/greeting

git branch

git switch feature/greeting

git switch -c feature/conflict
```
Branch를 이용하면 하나의 프로젝트에서 작업을 여러 흐름으로 나눌 수 있다.
```
main
 ├── feature/greeting
 └── feature/conflict
```
git switch -c는 브랜치를 생성하면서 해당 브랜치로 바로 이동한다.

### 8. Merge
```
git switch main
git merge feature/greeting
```
feature/greeting 브랜치에서 작업한 내용을 main 브랜치에 병합한다.

### 9. Merge Conflict
같은 파일의 같은 부분을 서로 다르게 수정한 상태에서 merge하면 충돌이 발생할 수 있다.
```
<<<<<<< HEAD
메인에서 수정한 내용
=======
브랜치에서 수정한 내용
>>>>>>> feature/conflict
```
충돌이 발생하면 원하는 내용을 직접 선택하고 충돌 표시를 삭제한 후:
```
git add .
git commit -m "충돌해결"
```
하여 충돌을 해결한다.

### 10. Fork → Clone → Branch → Push → Pull Request
다른 사람의 GitHub 프로젝트에 기여할 때 사용하는 기본적인 흐름이다.
```
원본 Repository
      ↓ Fork
내 GitHub Repository
      ↓ Clone
내 컴퓨터
      ↓ Branch 생성
작업 및 수정
      ↓
git add .
      ↓
git commit
      ↓
git push
      ↓
Pull Request
      ↓
원본 Repository에 Merge
```
```
Clone:

git clone https://github.com/nahyebin/git-practice.git
```
```
Branch 생성:

git switch -c feature/my-name
```
```
작업 후 commit:

git add .
git commit -m "nahyebin 파일 추가"
```
```
원격 저장소에 branch push:

git push origin feature/my-name
```
이후 GitHub에서 Pull Request를 생성하여 원본 Repository에 병합을 요청한다.

## 느낀점
오늘 Git과 GitHub를 처음부터 직접 실습하면서 각각의 명령어가 어떤 역할을 하는지 이해할 수 있었다. 특히 git add → git commit → git push의 흐름과 git pull을 통해 로컬과 원격 저장소가 어떻게 연결되는지 알게 되었다.

또한 Branch를 생성해서 작업을 분리하고 Merge하는 과정과 실제로 Merge Conflict가 발생했을 때 직접 충돌을 해결하는 과정까지 경험해보면서 Git을 사용하는 이유를 조금 더 이해할 수 있었다.

마지막으로 Fork, Clone, Branch, Push, Pull Request까지 실습하면서 여러 명이 하나의 프로젝트를 협업할 때 Git과 GitHub가 어떻게 사용되는지도 배울 수 있었다. 앞으로 프로젝트를 진행하면서 오늘 배운 Git 명령어들을 직접 사용해보며 익숙해져야겠다.
