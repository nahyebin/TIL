
# JavaScript 함수 & 스코프

## 오늘 배운 것
- 함수의 기본 구조와 `return`
- 매개변수(parameter)와 인수(argument)
- 함수 선언문과 함수 표현식의 차이
- 함수 호이스팅
- 콜백 함수와 고차 함수
- 스코프와 렉시컬 스코프
- `var`, `let`, `const`의 스코프 및 재선언/재할당 차이
- 호이스팅과 TDZ
- `const` 객체의 프로퍼티 변경

## 핵심 내용 (코드)

### 1. 함수 기본 구조

```javascript
function calculateArea(width, height) {
    const area = width * height;
    return area;
}

const result = calculateArea(10, 20);
console.log(result);
````

 함수는 특정 동작을 하나의 코드 묶음으로 만들어 재사용할 수 있게 한다.

 - 매개변수(parameter): 함수를 정의할 때 전달받을 값을 담는 변수
- 인수(argument): 함수를 호출할 때 실제로 전달하는 값
- `return`: 함수의 결과를 호출한 곳으로 반환

 ### 2\. return

```
function add(a, b) {
    return a + b;
}

const result = add(10, 20);

console.log(result);    // 30
console.log(result * 2); // 60
```

 `return`이 없는 함수는 호출 결과로 `undefined`를 반환한다.

```
function printAdd(a, b) {
    console.log(a + b);
}

const result = printAdd(10, 20);
console.log(result); // undefined
```

 또한 `return`을 만나면 함수가 즉시 종료되기 때문에 `return` 아래의 코드는 실행되지 않는다.

 ### 3\. Early Return

 조건을 만족하지 않을 경우 함수를 바로 종료하는 방식이다.

```
function registerUser(nickname) {
    if (nickname.length < 2) {
        console.log('닉네임이 너무 짧습니다');
        return;
    }

    console.log(`${nickname}님 환영합니다~`);
}
```

 조건을 통과하지 못하면 `return`으로 함수를 종료하고, 통과한 경우에만 메인 로직을 실행한다.

 ### 4\. 함수 선언문과 함수 표현식

```
// 함수 선언문
function hello(name) {
    return `${name} 안녕?`;
}

// 함수 표현식
const hi = function(name) {
    return `${name}님 안녕하세요!`;
};
```

 함수 선언문은 호이스팅의 영향을 받기 때문에 함수 선언 전에 호출할 수 있다.

```
console.log(hello('홍길동'));

function hello(name) {
    return `${name} 안녕?`;
}
```

 반면 함수 표현식은 변수에 함수가 할당된 이후에 사용할 수 있다.

```
// hi('판다'); // ReferenceError

const hi = function(name) {
    return `${name}님 안녕하세요!`;
};
```

 함수 표현식은 함수 이름을 생략할 수 있으며, 일반적으로 함수 선언문보다 함수 표현식을 더 선호한다.

 ### 5\. 함수는 값이다

 JavaScript에서는 함수도 하나의 값이기 때문에 변수에 저장하거나 객체의 프로퍼티로 사용할 수 있다.

```
const sayHello = function() {
    console.log('안녕하세요~');
};

sayHello();
```

 객체의 프로퍼티 값으로 함수를 넣으면 메서드가 된다.

```
const myObject = {
    sayHi: function() {
        console.log('반갑습니다!');
    }
};

myObject.sayHi();
```

 ### 6\. 콜백 함수와 고차 함수

 함수는 다른 함수에 인수로 전달할 수 있다.

```
function manager(task) {
    console.log('매니저가 업무를 지시합니다.');
    task();
}

function sayHello() {
    console.log('안녕하세요~');
}

manager(sayHello);
```

 - 콜백 함수: 다른 함수에 전달되어 필요한 시점에 호출되는 함수
- 고차 함수: 함수를 인수로 받거나 함수를 반환하는 함수

```
function calculator(calculateCallback, a, b) {
    const result = calculateCallback(a, b);
    return result;
}

function add(a, b) {
    return a + b;
}

function multiply(a, b) {
    return a * b;
}

console.log(calculator(add, 9, 8));
console.log(calculator(multiply, 9, 8));
```

 ### 7\. 스코프

 스코프는 변수를 사용할 수 있는 범위이다.

```
function calculateArea(width, height) {
    const area = width * height;
    return area;
}

const result = calculateArea(10, 20);

// console.log(area); // ReferenceError
```

 함수 내부에서 선언한 변수는 함수 밖에서 사용할 수 없다.

 `let`, `const`는 블록 스코프를 가진다.

```
const outerValue = '바깥쪽 값';

if (true) {
    const blockValue = '블록 안의 값';

    console.log(outerValue);
    console.log(blockValue);
}

// console.log(blockValue); // ReferenceError
```

 안쪽에서는 바깥쪽 스코프의 변수에 접근할 수 있지만, 바깥쪽에서는 안쪽 스코프의 변수에 접근할 수 없다.

 ### 8\. 렉시컬 스코프

 JavaScript는 함수가 호출된 위치가 아니라 **함수가 정의된 위치**를 기준으로 상위 스코프를 결정한다.

```
const label = '바깥';

function printLabel() {
    console.log(label);
}

function run() {
    const label = 'run 안쪽';
    printLabel();
}

run(); // 바깥
```

 `printLabel()`은 `run()` 안에서 호출되었지만 `printLabel`이 정의된 위치를 기준으로 `label`을 찾기 때문에 `'바깥'`이 출력된다.

 ### 9\. var, let, const 차이

```
var oldMessage = '처음';
var oldMessage = '변경'; // 재선언 가능

let message = '처음';
message = '변경'; // 재할당 가능

const greeting = '안녕하세요';
// greeting = '안녕히 가세요'; // 재할당 불가능
```

 | 구분 | `var` | `let` | `const` |
| --- | --- | --- | --- |
| 재선언 | 가능 | 불가능 | 불가능 |
| 재할당 | 가능 | 가능 | 불가능 |
| 스코프 | 함수 스코프 | 블록 스코프 | 블록 스코프 |

 기본적으로 `const`를 사용하고, 재할당이 필요한 경우 `let`을 사용한다.

 ### 10\. 호이스팅과 TDZ

 `var`는 선언이 끌어올려진 것처럼 동작한다.

```
console.log(oldValue); // undefined

var oldValue = '준비됨';
```

 반면 `let`, `const`는 선언 전에 접근할 수 없다.

```
// console.log(value); // ReferenceError

let value;
```

 `let`, `const`는 선언부터 초기화되기 전까지의 구간을 \*\*TDZ(Temporal Dead Zone)\*\*라고 한다.

 ### 11\. const와 객체

 `const`로 선언한 객체는 객체 자체를 재할당할 수 없지만, 객체 내부의 프로퍼티는 변경할 수 있다.

```
const student = {
    name: '판다',
    age: 5
};

student.name = '코알라';

console.log(student.name); // 코알라
```

```
// 객체 자체의 재할당은 불가능
// student = { name: '홍길동', age: 30 };
```

 즉, `const`는 객체 내부의 값까지 변경할 수 없게 만드는 것이 아니라 **변수가 다른 객체를 가리키도록 재할당하는 것을 막는다.**