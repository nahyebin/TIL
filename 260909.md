
# JavaScript 핵심 개념 정리

## 1. 옵셔널 체이닝 ?.
`?.`는 객체가 `null` 또는 `undefined`인지 확인한 후 안전하게 프로퍼티에 접근한다.

```javascript
const obj = null;
console.log(obj?.value); // undefined

```

일반적인 접근은 에러가 발생한다.

```javascript
console.log(obj.value); // TypeError

```

**핵심**

* `?.` = 있으면 접근하고, 없으면 `undefined`
* 중요한 점은 Falsy가 아니라 `null`과 `undefined`만 확인한다는 것이다.

---

## 2. &&와 ?.의 차이

`&&`는 앞의 값이 Falsy이면 거기서 멈춘다.

```javascript
const str = '';
console.log(str && str.length); // ''
console.log(str?.length);       // 0

```

| 값 | `&&` | `?.` |
| --- | --- | --- |
| `null` | 멈춤 | `undefined` |
| `undefined` | 멈춤 | `undefined` |
| `''` | 멈춤 | 접근 가능 |
| `0` | 멈춤 | 접근 가능 |
| `false` | 멈춤 | 접근 가능 |

**핵심**

* `&&` → Falsy 기준
* `?.` → `null` / `undefined` 기준

---

## 3. null 병합 연산자 ??

`??`는 좌항이 `null` 또는 `undefined`일 때만 우항을 사용한다.

```javascript
const a = null ?? '기본값';
const b = undefined ?? '기본값';
const c = '' ?? '기본값';
const d = 0 ?? '기본값';

console.log(a); // 기본값
console.log(b); // 기본값
console.log(c); // ''
console.log(d); // 0

```

**핵심**

* `??` = `null` / `undefined`일 때만 기본값

---

## 4. ||와 ??의 차이

`||`는 Falsy 전체를 기준으로 판단한다.

`??`는 `null` / `undefined`만 기준으로 판단한다.

```javascript
console.log('' || '기본값');   // 기본값
console.log('' ?? '기본값');   // ''
console.log(0 || 100);        // 100
console.log(0 ?? 100);        // 0
console.log(false || true);   // true
console.log(false ?? true);   // false

```

**핵심**

* `||` → Falsy면 기본값
* `??` → `null` / `undefined`면 기본값

따라서 `0`, `''`, `false`도 정상적인 값으로 취급해야 한다면 `??`를 사용하는 것이 적합하다.

---

## 5. 생성자 함수와 new

생성자 함수는 비슷한 구조의 객체를 여러 개 만들기 위한 함수이다.

```javascript
function Student(name, age) {
    this.name = name;
    this.age = age;
}

const student1 = new Student('판다', 8);
const student2 = new Student('코알라', 5);

```

`new`를 사용하면 새로운 객체가 만들어지고, 생성자 함수 내부의 `this`가 새 객체를 가리킨다.

* 생성자 함수 = 객체를 만드는 틀
* `new` = 그 틀을 이용해 새로운 객체 생성

---

## 6. new의 핵심 동작

```javascript
const student = new Student('판다', 8);

```

개념적으로 다음 과정이 일어난다.

```text
새로운 객체 생성
      ↓
this가 새 객체를 가리킴
      ↓
생성자 함수 실행
      ↓
Student.prototype과 연결
      ↓
완성된 객체 반환

```

즉,

```text
new Student()
      ↓
새 객체 생성
      ↓
this 연결
      ↓
프로퍼티 초기화
      ↓
Student.prototype 연결
      ↓
객체 반환

```

**핵심**

* `new` = 객체 생성 + `this` 연결 + `prototype` 연결 + 반환

---

## 7. this는 호출 주체가 중요하다

```javascript
const user = {
    name: '판다',
    hello() {
        console.log(this.name);
    }
};

user.hello(); // 판다

```

`user.hello()`에서 `.` 앞의 객체인 `user`가 `this`가 된다.

프로토타입에 있는 메서드도 마찬가지다.

```javascript
student.login();

```

메서드가 `prototype`에 정의되어 있더라도

```text
this → student

```

가 된다.

**핵심**

* `this`는 함수가 어디에 정의되어 있는지보다 **어떻게 호출되는지**가 중요하다.

---

## 8. 프로토타입과 프로토타입 체인

JavaScript 객체는 다른 객체를 `prototype`으로 연결할 수 있다.

```javascript
const user = {
    id: 'user',
    activate: true
};

const student = Object.create(user);

```

구조는 다음과 같다.

```text
student
   ↓
  user
   ↓
  null

```

`student`에 없는 프로퍼티를 찾으면 `prototype`으로 올라가며 탐색한다.

```javascript
console.log(student.activate); // true

```

`student`에는 `activate`가 없지만 `user`에 있기 때문이다.

**프로퍼티 탐색 순서**

```text
내 객체
   ↓
prototype
   ↓
그 위의 prototype
   ↓
...
   ↓
null

```

---

## 9. prototype, hasOwn, in

생성자 함수의 `prototype`에 공통 메서드를 넣을 수 있다.

```javascript
function Student(name, age) {
    this.name = name;
    this.age = age;
}

Student.prototype.getInfo = function() {
    return `${this.name}는 ${this.age}세 입니다.`;
};

const student1 = new Student('판다', 8);
const student2 = new Student('코알라', 5);

```

`getInfo`는 각 객체에 따로 존재하는 것이 아니라 `Student.prototype`에 존재한다.

```javascript
Object.hasOwn(student1, 'name');    // true
Object.hasOwn(student1, 'getInfo'); // false
'getInfo' in student1;              // true

```

**차이**

* `Object.hasOwn()` → 객체 자신이 직접 가지고 있는가?
* `in` → `prototype`까지 포함해서 존재하는가?

**핵심 암기**

* `hasOwn` = 내 것인가?
* `in` = 어디엔가 존재하는가?

---

## 10. delete

`delete`는 객체가 직접 가지고 있는 프로퍼티를 삭제한다.

```javascript
delete student.name;

```

`prototype`에 같은 이름의 프로퍼티가 있다면 그것까지 삭제되지는 않는다.

객체 구조는 다음과 같다.

```text
student
   ↓
Student.prototype
   ↓
Object.prototype
   ↓
null

```

`delete`는 해당 객체가 직접 가지고 있는 프로퍼티를 삭제할 뿐, `prototype`의 프로퍼티까지 삭제하지 않는다.

---

## 전체 개념 연결

최종적으로 오늘 배운 개념은 하나의 흐름으로 연결된다.

```text
생성자 함수
    ↓
   new
    ↓
새 객체 생성
    ↓
   this
    ↓
객체의 프로퍼티 초기화
    ↓
prototype 연결
    ↓
프로토타입 체인으로 프로퍼티 탐색
    ↓
객체.메서드() 호출
    ↓
this는 호출한 객체

```

---

## 핵심 암기

| 개념 | 핵심 |
| --- | --- |
| `?.` | `null` / `undefined`면 안전하게 접근 |
| `&&` | Falsy면 멈춤 |
| `??` | `null` / `undefined`면 기본값 |
| `new` | 새로운 객체 생성 |
| `this` | 호출 주체를 가리킴 |
| `Object.create()` | `prototype` 연결 |
| `prototype` | 객체들이 공유하는 기능 |
| `hasOwn` | 자신의 프로퍼티인지 확인 |
| `in` | `prototype`까지 포함해 확인 |
| `delete` | 객체의 프로퍼티 삭제 |
